{
  "lens": "L2",
  "verdict": "accept",
  "score": 9,
  "blocking": [],
  "non_blocking": [
    {
      "section": "Filter Chain execution / Short-circuit",
      "suggestion": "L255 says 'In `onUpstreamRequest` a short-circuit ends only that leg, refining pack 4 for multi-leg Routes'. Pack 4 is binding: 'skipping to `onResponse` and `onLog`'. Pack 14 requires an ADR or an Open question for any change after the freeze. Every other deviation in this document now has one (OQ-data-plane-8, -9, -12 and -13), but this one does not. Add OQ-data-plane-14: 'Should pack 4 record that a short-circuit in `onUpstreamRequest` ends only that upstream leg, and that a composition step records the generated response as its result?' Options: (a) amend pack 4 (current); (b) keep pack 4, so a leg short-circuit ends the whole request. Owner: data-plane. Blocking?: 'Yes, pack 4 amendment (pack 14)'."
    },
    {
      "section": "Failure semantics (auth and authz rows)",
      "suggestion": "L421 and L422 now include `plugin` with `filterClass: auth` or `authz`, yet the 'Response Phase before commit' cell still says 'Not applicable'. That holds for the built-in types, which run only in request Phases. A Plugin's `phases` may include `onResponse` or `onUpstreamResponseBody`, and pack 8.10 gives 'Replace the response with 502' there. Write 'Not applicable to built-in types; `plugin`: 502 `RZ-PLG-<NNN>` (closed only)'."
    },
    {
      "section": "Filter Chain execution / Error handling",
      "suggestion": "L299 lists 'a runtime error in a Policy `config` CEL field' as a cannot-decide case governed by `failureMode`. The next sentence sends only 'Other CEL fields' to the Configuration model's CEL table. That table is normative, and it gives some `config` fields their own rule: `cache` and `ai.semantic-cache` `config.key` bypass the cache, and `authz.cel` `config.rule` denies. The Failure semantics table already gets `config.key` right, but the Error handling sentence contradicts it under `closed`. Reword it as: 'a runtime error in a CEL field whose runtime-error column says Policy `failureMode`; every other CEL field follows its own column'."
    },
    {
      "section": "Failure semantics / RZ-RT registry",
      "suggestion": "Carried over from the last round and still open. The Configuration model's gate row sets '502 for another response body' over a limit, and this document owns RZ-RT. RZ-RT-015 covers only composition steps, so a non-composition Route whose `transform.response`, or a Plugin in `onUpstreamResponseBody`, meets a body over `maxResponseBodyBytes` has no code. Widen RZ-RT-015 to 'a buffered response or step body over its cap, or a step CEL error', and it is then covered by OQ-data-plane-9."
    },
    {
      "section": "Composition engine / Failures and partial responses",
      "suggestion": "Carried over. '502 with the `RZ-UP-<NNN>` code of an Upstream cause' still covers a well-formed non-2xx step result and a generated response from an upstream-leg short-circuit, such as `auth.upstream-oauth2`. Neither is among the pack 8.6 `UP` causes (connect, TLS, timeout, reset, open breaker, retries exhausted, mid-stream failure). Name the code for each case, or say that Traffic management and resilience registers one."
    },
    {
      "section": "Composition engine / mode table and Merge rules",
      "suggestion": "Merge rules says merging 'match[es] the Configuration model's gate list', but that list also gates a step 'read later through `steps`', and such a step is buffered, not merged. Also, in `sequential`, a step that is not the last executed one but sets `group` merges into nothing, because the mode row says earlier results reach later steps only through `steps`. Write 'Merging applies to `aggregate` steps and to the response step of `sequential` or `conditional` when it sets `target`, `select`, `rename` or `group`; a step read later through `steps` is buffered but not merged'."
    },
    {
      "section": "Router / Precedence",
      "suggestion": "Carried over. 'falling through on a `methods` mismatch' (L186) is narrower than Figure 2, which also falls through on `headers`, `grpc` and a false `when`. Write 'falling through when `methods`, `headers`, `grpc` or `when` does not match'."
    },
    {
      "section": "Process and concurrency model / Goroutines and Plugin host",
      "suggestion": "Carried over. System overview's 'Bounded everything' row names a 'per-key bucket table (LRU)', so add '(an LRU approximation)' after CLOCK eviction. The 1 ms (target) pool-acquisition wait sits inside territory the Configuration model and this section give to WASM plugin system, so write 'at most 1 ms (target, proposed to WASM plugin system)'."
    },
    {
      "section": "Listeners and protocols / Bounded resources",
      "suggestion": "`HTTP2Config.MaxReceiveBufferPerConnection` and `MaxReceiveBufferPerStream` (L120) cite https://go.dev/doc/go1.24. The research file for this document mentions only `HTTP2Config` in general there. The field names and valid ranges are in docs/_meta/research/scalability-patterns.md (https://raw.githubusercontent.com/golang/go/master/src/net/http/http.go). Cite that URL, or have go-libraries-runtime.md add the fields. The claim that the goroutine leak profile is GA 'in Go 1.27' is correct, but it applies to release builds only: Go 1.26 floor builds keep it behind `GOEXPERIMENT`. Say so."
    },
    {
      "section": "Configuration snapshots and hot reload / Activation",
      "suggestion": "Step 1 ends 'and trust policy `off` is a degraded state' without saying where that applies. Pack 8.14 allows `off` only for OCI-published Revisions and Plugin artifacts. A Control-mode Revision is 'Always verified; failure is a NACK'. Write 'trust policy `off`, allowed only for OCI Revisions and Plugin artifacts, is a degraded state'."
    }
  ],
  "ac_results": [
    {
      "ac": "Filter Chain execution names the phases exactly as in the foundation pack, in order, including onChunk, and covers ordering, short-circuit, streaming and error handling.",
      "pass": true,
      "note": "L235 quotes the pack 4 sequence verbatim, plus `onChunk`. The Ordering, Short-circuit, Streaming and Error handling H3s and Figure 3 cover the rest. The Filter class order matches pack 8.12. The onUpstreamRequest refinement needs an OQ (non-blocking), and so does the Error handling wording (non-blocking)."
    },
    {
      "ac": "Configuration snapshots and hot reload explains how an atomic snapshot swap drops no in-flight requests (in-flight requests complete on the snapshot they started on).",
      "pass": true,
      "note": "'Why no in-flight request is dropped' covers striped pins with a re-load, closing only resources, listeners that stay bound, and bounded retirement at K = 2 (target) with a 30 s grace period (target). Figure 4 is the stateDiagram-v2. Previous L2-1 is resolved: L393 now defers boot order to pack 8.2 and System overview's Last-Known-Good, with no misstated else chain, and names both file sources. The System overview deviations are routed through OQ-data-plane-12 (GOAWAY) and OQ-data-plane-13 ('activation never waits')."
    },
    {
      "ac": "Router includes a routing precedence worked example covering host, path, method and wildcard matches.",
      "pass": true,
      "note": "The example has six Routes and eight requests. I rechecked every winner against the rank table: exact beats template, a methods mismatch falls through to the prefix, the wildcard host matches, the bare suffix misses, and `api.shop.example/health` reaches `fallback` through the wildcard tier."
    },
    {
      "ac": "The streaming path is described for SSE, WebSocket and LLM token streams via onChunk.",
      "pass": true,
      "note": "The Streaming H3 has one bullet each for SSE, WebSocket and LLM token streams, all Planned (M3). It also covers the chunk cap (RZ-RT-013), flow control and the rule that `onChunk` never calls the State Store (pack 8.7)."
    },
    {
      "ac": "The document states that Ruralz Gateway keeps no durable local state other than its enrollment identity, Last-Known-Good configuration and disposable caches (foundation pack section 8.11), and that shared runtime state lives in the State Store.",
      "pass": true,
      "note": "L34 states it and cites pack 8.11. The overbroad lock sentence from the last round has been removed."
    },
    {
      "ac": "Admin endpoints lists at least /healthz, /readyz, /metrics, /debug/*, /config/dump and /tap on port 9901, and is the source that docs/reference/01-cli-and-api-surface.md must match.",
      "pass": true,
      "note": "All six paths are listed on 9901, with `/debug/*` expanded, and L399 says 'CLI and API surface MUST match this table'. Authentication matches pack 8.4 and System overview TB-9 ('token or mTLS'). `Gateway.spec.admin.port` exists in the Configuration model. `ruralz node dump`, `ruralz bundle diff` and `ruralz dev tap` are in the pack 9 registry."
    },
    {
      "ac": "Failure semantics defines fail-open or fail-closed behavior per filter class in a table.",
      "pass": true,
      "note": "All nine Filter classes have a row. Previous L2-2 is resolved: upstream-auth gives 401 `RZ-AUTH-<NNN>` per pack 8.10, and OQ-data-plane-8 tags (a) 401 as current, with the pack amendment marked blocking. Previous L2-3 is resolved: the auth and authz rows include `plugin` with those classes, custom is `filterClass: custom` (the default), and L430 keeps the plugin default at `closed` per pack 10. Previous L2-4 is resolved: OQ-data-plane-9 covers RZ-RT-011 to 015 and the registry points to it. The 'Not applicable' response-Phase cell for auth and authz plugins is non-blocking."
    },
    {
      "ac": "Composition engine covers aggregation, sequential, conditional and workflow composition.",
      "pass": true,
      "note": "All four are covered. `sequential` now returns the last executed step's response, streamed unless that step transforms it, which matches the Configuration model's gate list. Workflow composition is OQ-data-plane-3, with no invented field."
    },
    {
      "ac": "Performance budgets points to docs/architecture/12-performance-budgets-and-benchmarking.md instead of restating budgets.",
      "pass": true,
      "note": "It links document 12, which wins on conflict, restates no budget, and lists the design constants that suite verifies."
    },
    {
      "ac": "H2 outline equals mandatory first two + outline_h2 + Open questions, exactly and in order.",
      "pass": true,
      "note": "All 13 H2s match exactly and in order."
    },
    {
      "ac": "Required diagrams: sequenceDiagram (listener to Upstream and back), flowchart (Filter Chain ordering and short-circuit), stateDiagram-v2 (snapshot swap), each with an italic caption.",
      "pass": true,
      "note": "Figures 1, 3 and 4 are present, each with an italic caption directly above it. Figure 2 is an extra Router flowchart."
    },
    {
      "ac": "Machine checks and verify-docs FAIL lines (required_terms, has_table x2, links_to, length, Summary words).",
      "pass": true,
      "note": "`node scripts/verify-docs.mjs --allow-missing --skip markdownlint` prints no FAIL line for this document. The 18 WARN lines are broken links to documents and ADRs not yet written. Every link target is a manifest path, and every anchor into System overview and the Configuration model exists."
    },
    {
      "ac": "Canonical names, no forbidden aliases, Planned (Mx) tags only, research-cited claims, numbers tagged.",
      "pass": true,
      "note": "The forbidden-alias patterns and the pack 2 'never write' list find nothing, and 'Supported' does not appear. All nine URLs are in docs/_meta/research/go-libraries-runtime.md and agree with it: wazero cache, 4 GiB default, Call not goroutine-safe, h2c, StrictMaxConcurrentRequests, x/net deprecation, quic-go v0.63.0 and goroutineleak GA. The names of the two HTTP/2 receive-buffer fields are sourced only in scalability-patterns.md (non-blocking)."
    },
    {
      "ac": "Consistency with the foundation pack, System overview and Configuration model (kinds, fields, codes, ports, CLI, metric and span names, OQ references, front matter).",
      "pass": true,
      "note": "Every YAML field used exists in the Configuration model: listeners, admin.port, the limits.* fields, composition step fields, loadBalancing, healthCheck, retries, circuitBreaker, filterClass and timeout. RZ-CFG-023, 026, 027, 028, 029 and 033 match its registry. The span names follow pack 2, and the metric names follow `ruralz_<component>_<name>_<unit>`, marked proposed to Observability. These cross-document OQ references exist with matching subjects: system-overview-5, -6, -18; configuration-model-11; tech-stack-and-libraries-15, -16, -23. `coder/websocket` has a tech stack catalog row. The front matter `adrs` is a superset of the manifest's five ADRs and covers every ADR cited, and `milestone_tags_used` [M1..M4] matches the body. All OQ owners are manifest slugs. The remaining deviations from System overview are tracked by OQ-data-plane-12 and -13. The pack 4 refinement still needs an OQ (non-blocking)."
    }
  ],
  "foundation_amendments": [
    "Pack 8.10, tracked as OQ-data-plane-8: decide whether `auth.upstream-oauth2` and `auth.upstream-sigv4` keep 401 on a fail-closed error or move to 503 or 502, since the client is not at fault.",
    "Pack 8.6, tracked as OQ-data-plane-9: widen `RT` to 'request and response handling on a Node outside Upstream legs', to cover RZ-RT-011 to RZ-RT-015.",
    "System overview, Compile before swap, tracked as OQ-data-plane-12 and -13: replace 'HTTP/2 GOAWAY' with per-stream closes (gRPC trailers `UNAVAILABLE`, then `RST_STREAM` after the grace period), and replace 'activation never waits' with 'the latest pending Revision waits until the closing snapshot is freed, at most the grace period plus compile time (target)'.",
    "Pack 4, not yet tracked by an OQ: record that a short-circuit in `onUpstreamRequest` ends only that upstream leg, and that a composition step records the generated response as its result."
  ]
}
