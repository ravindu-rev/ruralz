{
  "lens": "L2",
  "verdict": "accept",
  "score": 9,
  "blocking": [],
  "non_blocking": [
    {
      "section": "Decision drivers (line 29)",
      "suggestion": "The driver label already says \"(P7 exception, OQ-multi-protocol-18)\", and the sentence then repeats \", a P7 exception in the owning document until OQ-multi-protocol-18 closes\". An HTTP publish is an ordinary request, so it is not part of the exception. Delete the repeated clause and keep the label. The label is the text that resolved L2-1, and it stays. This saves about 12 words for the item below."
    },
    {
      "section": "Whole document (length)",
      "suggestion": "The body is about 1,496 words by the manifest word_count_rule, against the adr_common maximum of 1,500. The verifier passes today. Any later fix will cross the limit unless it is paid for, for example with the line 29 trim above. Do not remove the P7-exception qualifiers on lines 19, 29, 43 and 142, or the `ProducerLinger(0)` wording on line 47. Those resolve L2-1 and L2-2."
    },
    {
      "section": "Decision outcome, Wire protocols row (line 51)",
      "suggestion": "Style guide section 4 requires a reason with every `Not planned`. The Kafka reason is under Pros and cons, but NATS proxying has none. Add the owning document's reason: \"NATS proxying Not planned, since NATS has its own clustering and authorization\"."
    },
    {
      "section": "Decision outcome > Confirmation (line 103)",
      "suggestion": "The \"Quarterly health review\" bullet is the only Confirmation mechanism without a milestone tag. The tech stack document's S1 check is the quarterly health review. Tag it as the other bullets are, for example \"from Planned (M0)\" or \"from M4\"."
    },
    {
      "section": "More information (line 147)",
      "suggestion": "Repository layout and conventions keeps library confinement rules under Banned imports: wazero to `internal/pluginhost` and `hashicorp/raft` to `internal/controlstore/raft`. Its Where Go code goes list says depguard confines three libraries. The proposed depguard amendment would therefore sit under `#banned-imports` rather than `#import-boundaries`. Consider linking that anchor, or naming both."
    },
    {
      "section": "Acceptance criterion 6",
      "suggestion": "docs/adr/README.md does not exist yet (the verifier's --allow-missing covers it). The owning document links ADR-0013 in its Kafka, NATS JetStream and MQTT table and in its Native wire-proxy stance table. The index generator (adr_index) must list ADR-0013 when it writes the README. The fix is outside this ADR."
    }
  ],
  "ac_results": [
    {
      "ac": "Front matter contains id, title, status, date, deciders and related, and status matches this manifest.",
      "pass": true,
      "note": "All six keys are present. The id is ADR-0013, the title matches the manifest title exactly, `status: accepted` matches the adrs[] entry, and the date is 2026-09-25. Every related path exists."
    },
    {
      "ac": "H2 headings match adr_common.outline_h2 exactly and in order, and Decision outcome contains the H3 headings Consequences and Confirmation.",
      "pass": true,
      "note": "The six H2 headings are exact and in order. Consequences (line 85) and Confirmation (line 98) are H3 headings under Decision outcome. The verifier prints no FAIL line for this file."
    },
    {
      "ac": "Considered options lists at least two options including the chosen one, and each has pros and cons.",
      "pass": true,
      "note": "There are seven options, including the chosen one, and each cites a research URL. The Pros and cons H3 headings repeat the option titles exactly, and every option has Good and Bad bullets. Option 7 is now rejected on grounds that fit P7: records would reach Policies only as `onChunk`, where `authz.*`, `ratelimit` and `quota` do not run (pack section 10 Phases)."
    },
    {
      "ac": "Decision outcome states the chosen option as 'Chosen option: ..., because ...' and matches the foundation pack section 7 row for this ADR.",
      "pass": true,
      "note": "Line 43 uses the required form and quotes the option 1 title exactly. The four libraries, the embedded broker at Planned (M4) and no native Kafka or MQTT wire proxying before M4 match the pack section 7 Messaging row and the manifest decision. The because-clause carries the P7-exception qualifier (L2-1 resolved)."
    },
    {
      "ac": "Confirmation names a concrete verification mechanism (test, lint rule, CI gate or review checklist item).",
      "pass": true,
      "note": "It names testcontainers-go integration tests in `pr-full` (Planned (M4)) with concrete assertions, the embedded broker spike, the G1 cross-build and G2 license gate (Planned (M0)), the quarterly health review, `govulncheck` (Planned (M0)) and a review checklist item from M4."
    },
    {
      "ac": "The ADR is linked from docs/adr/README.md and from its owning document.",
      "pass": false,
      "note": "docs/architecture/07-multi-protocol.md links ADR-0013 twice, in the Kafka, NATS JetStream and MQTT table and in the Native wire-proxy stance table. docs/adr/README.md has not been generated yet, so the index half fails. The fix is outside this ADR, so it is not a blocking item here."
    }
  ],
  "foundation_amendments": []
}
