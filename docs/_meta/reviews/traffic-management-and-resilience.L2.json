{
  "lens": "L2",
  "verdict": "revise",
  "score": 8,
  "blocking": [
    {
      "id": "L2-1",
      "section": "Rate limiting (Decision path); Failure matrix",
      "issue": "Prior finding L2-4 is fixed on line 219, step 3 and the Figure 3 edge, but the same problem remains in text that the fix did not touch. Line 219 says that until OQ-traffic-management-and-resilience-20 closes, the conforming behavior is option (c), wait for GCRA, which leaves first-seen calls 'bounded only by offered load'. Several statements still give the results of the proposed option (a) as the default, with no qualification. (1) Line 221 states unconditional first-seen call bounds. Under option (c) those bounds do not hold. (2) Line 223 says a new Node admits at the per-Node ceiling while it warms. Under option (c), keys past the budget wait for GCRA. (3) Failure matrix row 485 ('New or restarted Node') gives 'admitting at the per-Node ceiling without GCRA' as the Default behavior, with no 'proposed' tag. Row 484, which was split from the old combined row, is tagged. (4) Rows 484 and 495 give the option (a) first-seen bounds (target, hypothesis) as defaults. Pack 8.8 requires GCRA for every locally admitted request, and pack section 14 makes deviations review-blocking until they close. The Failure matrix is where operators look up default behavior, so it must show the conforming behavior.",
      "evidence": "line 219: 'until that closes, option (c), wait for GCRA, is the conforming behavior, leaving first-seen calls bounded only by offered load.'; line 221: 'With a count, first-seen calls per Cell are at most min(B × N_serving / N_published, 500 × N_serving) per second: B at steady state, 50,000 when 100 Nodes serve on a published 10 (hypothesis).'; line 223: 'A Node new from scale-out, a restart or a Zero-Downtime Upgrade starts empty and warms at its budget: ... meanwhile admitting at the per-Node ceiling, not below it.'; line 485: '| New or restarted Node, established keys | `ratelimit` | Warm in about 250 s for 50,000 keys at 100 Nodes (hypothesis), meanwhile admitting at the per-Node ceiling without GCRA | Open | OQ-traffic-management-and-resilience-20, -22 |'; line 484: 'First-seen calls at most min(B × N_serving / N_published, 500 × N_serving) per second, 200 × N without a count (target); later keys go local-only (proposed)'; line 495: 'make 50,000 first-seen calls per second for about 34 minutes (hypothesis)'; foundation pack 8.8: 'each locally admitted request runs GCRA in one Lua `EVAL` in the State Store'",
      "fix": "Line 221: begin with 'Under the proposed local-only entries (OQ-traffic-management-and-resilience-20 option (a)), first-seen calls per Cell are at most ...', and add 'under option (c), the conforming behavior until it closes, they are bounded only by offered load'. Line 223: change 'meanwhile admitting at the per-Node ceiling, not below it' to 'meanwhile, under the proposed option (a), admitting past-budget keys at the per-Node ceiling without GCRA; under option (c) they wait for GCRA'. Row 485 Default behavior: 'Warm in about 250 s for 50,000 keys at 100 Nodes (hypothesis); past-budget keys wait for GCRA until OQ-traffic-management-and-resilience-20 closes, or, if option (a) is adopted (proposed), admit at the per-Node ceiling without GCRA'. Row 484: add 'under option (a) (proposed); under option (c), bounded only by offered load' after the first-seen bound. Row 495: qualify '50,000 first-seen calls per second' with 'under option (a) (proposed)'. Optionally add the conforming path to Figure 3: a 'first-seen past budget, conforming (c)' edge from `nk` to `lb` that continues to GCRA."
    }
  ],
  "non_blocking": [
    {
      "section": "Rate limiting (Decision path, step 5)",
      "suggestion": "Tech stack's Rate limiting row says '`EVALSHA` with `NOSCRIPT` fallback'. With rueidis, the usual reading is a second EVAL round trip. Step 5 instead applies `failureMode` on `NOSCRIPT` and reloads off the request path, which pack 8.7 rule 1 requires. Add one clause saying this is how this document reads Tech stack's 'fallback', not an in-request EVAL retry, so that implementers do not use the client's default EVALSHA-then-EVAL behavior. If Tech stack means an in-request EVAL, raise it as an Open question owned by tech-stack-and-libraries."
    },
    {
      "section": "Timeouts, deadlines, retries and hedging (Deadlines)",
      "suggestion": "Line 114 says 'After commit only the Route `timeout` and the idle limit below bound the body'. That limit is only this document's recommendation for OQ-data-plane-10, and Data plane lists option (a), no idle limit, as current. Write 'the proposed idle limit below (OQ-data-plane-10)' so that a recommendation is not stated as behavior."
    },
    {
      "section": "Response caching",
      "suggestion": "Line 369 lets `config.key` replace per-principal keying, so for example a whole Tier shares cache entries. Security and identity says 'sharing needs a field (OQ-security-and-identity-25)' and gives only (a) None and (b) Flag as options. This document owns `cache`, so propose `config.key` replacement explicitly as an option (c) of OQ-security-and-identity-25. Do not present it as settled while that question is open."
    },
    {
      "section": "Traffic shaping (KrakenD EE routing and traffic features)",
      "suggestion": "The Catch-all fallback row ranks the catch-all by `metadata.name` only. Under Data plane precedence rank 5, a Route with only `when` set sorts before any Route without hosts and path that has zero counted constraints (for example one matching only `graphql` or `topic`), and after Routes with two or more constraints. Reword to 'last among Routes without `hosts` and `path` when ranks 5 and 6 place it there ([Data plane](03-data-plane.md#precedence))'."
    },
    {
      "section": "Traffic shaping; Circuit breakers (citations)",
      "suggestion": "`https://www.krakend.io/docs/backends/circuit-breaker/` appears only in docs/_meta/research/krakend-config-model.md, and `https://www.krakend.io/features/` only in krakend-parity.md, krakend-config-model.md and licensing-landscape.md. None of these is in this entry's research[] (scalability-patterns.md only). Both URLs satisfy style guide section 4, which accepts any file under docs/_meta/research/. Consider asking for krakend-parity.md and krakend-config-model.md to be added to the manifest entry's research[] so the run rule is met exactly."
    },
    {
      "section": "Length",
      "suggestion": "Body prose is about 6,490 words against a 6,500 maximum. Any further fix will push it over the band. Coordinate with the manifest owner on the raised maximum, and do not trim text that resolved earlier blocking findings."
    }
  ],
  "ac_results": [
    {"ac": "Rate limiting contains an algorithm table comparing accuracy, cost, multi-node behavior and burst handling (ADR-0008).", "pass": true, "note": "The Algorithm choice table has Accuracy, State Store cost per request, Multi-Node behavior, Burst handling and Used by columns for five algorithms, and cites ADR-0008."},
    {"ac": "Every KrakenD EE routing and traffic feature (catch-all fallback, header and query dynamic routing, conditional routing, wildcard routes, URL rewrite, virtual hosts, client redirects, customizable HTTP circuit breaker, service rate limit, tiered rate limit, stateful rate limit, IP filtering, GeoIP) is mapped to a Ruralz mechanism with a milestone.", "pass": true, "note": "All 13 rows are present with a mechanism and a Planned (M1) or (M2) tag, and they match the krakend-parity.md EE rows."},
    {"ac": "Failure matrix gives fail-open or fail-closed behavior for each dependency failure, including State Store outage.", "pass": true, "note": "Every row has the Fail-open or fail-closed column filled, including State Store slow or unreachable, shard failure, out of memory, eviction and failover. Rows 484, 485 and 495 give proposed OQ-20 behavior as the default (blocking L2-1)."},
    {"ac": "Rate limiting specifies the IETF RateLimit-Policy and RateLimit response header contract.", "pass": true, "note": "draft-ietf-httpapi-ratelimit-headers-11 fields, q, w, r and t, Retry-After precedence, and omitted pk and qu are specified for 429 responses. Admitted responses are deferred to OQ-traffic-management-and-resilience-2, which is blocking for M1."},
    {"ac": "Quotas supports long windows and token-weighted units, including LLM tokens.", "pass": true, "note": "Fixed epoch-aligned windows such as 24h and 720h. The `tokens` unit goes through `ai.token-budget`, with pack 8.9 reservation and settlement."},
    {"ac": "Timeouts, deadlines, retries and hedging defines a retry budget.", "pass": true, "note": "Retry budget subsection: max(3, 20% of in-flight originals) (target), with an exhaustion counter and fields in OQ-5."},
    {"ac": "Response caching follows HTTP caching semantics including stale-while-revalidate.", "pass": true, "note": "RFC 9111 shared cache with RFC 5861 stale-while-revalidate and stale-if-error, stale limits, request directives, revalidation, and Figure 4."}
  ],
  "foundation_amendments": [
    "Pack 8.8: allow first-seen keys past a per-Node budget to take local-only entries at the per-Node ceiling without GCRA (OQ-traffic-management-and-resilience-20, owner scalability-and-distributed-state); until it closes, every GCRA-free admission path in this document must be marked proposed.",
    "Pack 8.8: replace the limit / N per-Node ceiling derivation with min(requests, max(10, ceil(2 × requests / N_published))) and a clamped, window-aligned fail-open refill (OQ-traffic-management-and-resilience-16)."
  ]
}
