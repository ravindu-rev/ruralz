{
  "lens": "L4C",
  "verdict": "revise",
  "score": 7,
  "blocking": [
    {
      "id": "L4C-1",
      "section": "Concept mapping / extra_config namespaces (backend/http/client)",
      "issue": "The `backend/http/client` row grades the whole namespace `equivalent`, which contradicts three things. First, it contradicts the document's own rule 4, because the row admits that `proxy_address` has no target before Planned (M5). Second, it contradicts the root `client_tls` row, which maps the same settings to the same `Upstream.spec.tls` target and grades them `approximate` because cipher and version settings have no field. Third, it asserts KrakenD redirect behavior that the research does not record. The research lists only the sub-key names (`send_body_on_redirect`, `client_tls`, `no_redirect`, `proxy_address`), with no defaults or semantics. So nothing supports the claim that a file with `no_redirect` false or absent behaves like Ruralz passing 3xx responses through. `send_body_on_redirect` is not mapped at all.",
      "evidence": "Line 230: '| `backend/http/client` | B | `client_tls` to `Upstream.spec.tls`; `no_redirect: true` is the Ruralz default, since 3xx responses pass through | `equivalent` | `proxy_address` per OQ-krakend-ee-parity-matrix-6, Planned (M5) ...'. Line 109: '| `client_tls` | `Upstream.spec.tls` (...) | `approximate` | Cipher and version settings have no Upstream field |'. Line 474 (rule 4): 'Items whose target is not in the running release are `manual`.' Research krakend-config-model.md section 5.5: '`backend/http/client` | B | CE subset since v2.11 (`send_body_on_redirect`). EE adds `client_tls`, `no_redirect`, `proxy_address` | Per-backend HTTP client' (no defaults or behavior recorded).",
      "fix": "Grade each sub-key separately. `client_tls` is `approximate`, as in the root row. `no_redirect: true` is `equivalent`. `no_redirect` false or absent is `approximate`, with the note 'KrakenD redirect handling is unresearched; Ruralz passes 3xx responses through'. `send_body_on_redirect` is reported unmapped as `approximate`. `proxy_address` is `manual` under rule 4, Planned (M5), OQ-krakend-ee-parity-matrix-6. The alternative is to add these defaults and behaviors to docs/_meta/research/krakend-config-model.md, with the http-client URL, and cite them."
    },
    {
      "id": "L4C-2",
      "section": "Concept mapping / KrakenD path entries and their targets; extra_config namespaces (auth/validator E); Built-in KrakenD EE plugins",
      "issue": "Several rows grade KrakenD behavior `equivalent` that neither research file records, and the rows cite no source. This is the same defect as the resolved L4C-8, which covered `safejson`, `qos/http-cache` and algorithms, in rows that fix did not reach. (1) The research does not contain the `{resp0_field}` placeholder syntax. It records only that `sequential_propagated_params` is an accepted key, not its semantics. (2) The research lists the built-in plugins `jwk-aggregator`, `url-rewrite` and `wildcard` by name only, yet the document grades their mapping `equivalent` with specific semantics: per-issuer JWK URLs, a GET or HEAD path rewrite, and prefix matching. (3) The `auth/validator` E row, graded `equivalent`, converts 'role and scope checks' into an `authz.cel` rule. The research records `auth/validator` only as 'JWT validation', with no role or scope fields.",
      "evidence": "Line 163: '| endpoint `proxy.sequential_propagated_params` and `{resp0_field}` placeholders | Step `pathExpression` over `steps.<name>.body` | `equivalent` | The importer rewrites each placeholder to a CEL expression'. Line 272: '| `jwk-aggregator` | Several `issuers[]` entries in one `auth.jwt` Policy | `equivalent` | Each issuer keeps its own `jwksUrl` |'. Line 275: '| `url-rewrite` | Composition step `path` or `pathExpression` | `equivalent` for `GET` and `HEAD` |'. Line 277: '| `wildcard` | `match.path.prefix` | `equivalent` |'. Line 177: 'role and scope checks become an `authz.cel` rule over `auth.claims` ... `equivalent` for a key-set URL'. Research section 3: 'Endpoint-scope `proxy` accepts `sequential`, `sequential_propagated_params`, ...' (no placeholder syntax). Research section 5.6: 'Built-in EE plugin sub-keys: `geoip`, `ip-filter`, `jwk-aggregator`, `redis-ratelimit`, `static-filesystem`, `url-rewrite`, `virtualhost`, `wildcard`' (names only). Research section 5.1: '`auth/validator` | S, E | CE | JWT validation (E)'.",
      "fix": "Handle these rows the way the L4C-8 fix handled `safejson`. Grade line 163 `approximate`, with the note 'placeholder syntax and propagation semantics are unresearched; the importer reports each placeholder it rewrites'. Grade `jwk-aggregator`, `url-rewrite` and `wildcard` `approximate`, with the note 'sub-key semantics unresearched (research records names only)'. In the `auth/validator` E row, keep `equivalent` for signature, issuer and audience validation from a key-set URL. Grade the role and scope rule `approximate`, with the note 'KrakenD role and scope fields are unresearched'. Alternatively, add these facts with their KrakenD doc URLs to docs/_meta/research/krakend-config-model.md and cite them on each row."
    }
  ],
  "non_blocking": [
    {
      "section": "extra_config namespaces (qos/ratelimit/router)",
      "suggestion": "The IP-keyed `client_max_rate` is now `manual`, but it is not security-flagged, as the previous fix proposed. Mark it security-flagged, or say why a shared bucket behind the load balancer is not a security item, so that Stage 1 step 2 visibly covers it."
    },
    {
      "section": "extra_config namespaces (auth/api-keys S)",
      "suggestion": "The research lists `hash` values `plain`, `fnv128`, `sha256` and `sha1`, plus `propagate_role`. The row grades `plain` and marks `fnv128`, `sha1` and a `salt` as manual. It says nothing about unsalted `sha256` or `propagate_role`. Grade both explicitly so that no setting is silently dropped."
    },
    {
      "section": "Import tool fidelity levels / Rules that hold at every level (rule 8)",
      "suggestion": "'An unresolved URL keeps Nodes not ready (RZ-CFG-026)' is true only for a Node with no active Revision. The Configuration model says the Node rejects the Revision (a NACK in Control mode) and keeps serving its active one. Reword to cover both cases."
    },
    {
      "section": "Core concepts (EE license file); Behavior changes to plan for (License)",
      "suggestion": "Both rows support the KrakenD license-file claim only through the parity matrix. The underlying URL (https://www.krakend.io/docs/enterprise/overview/license-file/) sits in docs/_meta/research/licensing-landscape.md, which is not in this document's research[]. Either cite that URL directly, or state that the parity matrix is the sourced authority."
    },
    {
      "section": "Migration gaps / Behavior changes to plan for",
      "suggestion": "The intro says 'These hold even at `equivalent` fidelity', but the 'Two client credentials on one path' and 'Client address' rows describe `manual` items. Reword it to 'These hold whatever the fidelity level'."
    },
    {
      "section": "verify-docs output",
      "suggestion": "No FAIL lines were emitted. The WARN lines about milestone items missing from the roadmap, and about the KrakenD '/__health/' path at line 735, are informational. The '/__health/' warning is a false positive, because the path is KrakenD's."
    }
  ],
  "ac_results": [
    {
      "ac": "Concept mapping maps every top-level KrakenD configuration key and at least 20 extra_config namespaces to Ruralz kinds and Policies (KrakenD endpoint to Route, backend to Upstream, extra_config namespaces to Policies).",
      "pass": true,
      "note": "The Top-level keys table covers all 36 root properties from research section 2. The namespace table has 86 rows covering every namespace in research section 5. The Core concepts table maps endpoint to Route and backend to Upstream."
    },
    {
      "ac": "Import tool fidelity levels defines the fidelity levels reported by ruralz bundle import krakend (Planned (M2)) and what each guarantees.",
      "pass": true,
      "note": "Four levels are defined, each with a guarantee, importer action and operator action, and the forwarding caveat is carried into `exact` and `equivalent`. The `backend/http/client` grade still violates rule 4 (L4C-1)."
    },
    {
      "ac": "Plugin migration covers both paths: Go plugins to WASM Plugins (TinyGo) or built-in Filters, and Lua to CEL or WASM Plugins.",
      "pass": true,
      "note": "Both subsections are present: the registerer table, the porting steps, the accessor-to-Host Function table, the Lua layer and pattern tables, and the TinyGo Go PDK."
    },
    {
      "ac": "Cutover runbook gives ordered steps for shadow traffic and side-by-side cutover, including rollback.",
      "pass": true,
      "note": "Stages 0 to 6, the shadowing rules, weighted steps, the relative p99 rollback trigger and a gate summary are all present."
    },
    {
      "ac": "Migration gaps is consistent with the Not planned rows of docs/comparison/01-krakend-ee-parity-matrix.md.",
      "pass": true,
      "note": "The document's 13 rows, three of them EE-only, match the parity matrix, where the six-broker row is expanded. The 17 partial-parity rows also match."
    },
    {
      "ac": "Quoted KrakenD terminology (endpoint, backend) carries the <!-- alias-ok --> escape.",
      "pass": true,
      "note": "Every prose or table line that quotes endpoint or backend carries the escape, and the H3 is renamed. The remaining unescaped occurrences are KrakenD JSON keys and RFC 6901 pointers inside code blocks."
    }
  ],
  "foundation_amendments": []
}
