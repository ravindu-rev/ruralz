{
  "lens": "L2 foundation-auditor",
  "verdict": "revise",
  "score": 8,
  "blocking": [
    {
      "id": "L2-1",
      "section": "Library catalog",
      "issue": "Carried over from the previous L2 verdict (then L2-5); still not resolved. Acceptance criterion 1 requires every concern, including docs tooling (MADR 4.0, markdownlint-cli2, lychee, mermaid-lint), to have a catalog row whose source URL appears in docs/_meta/research/. The research files have not changed since 16:54 on the snapshot date and contain no URL for any of the four tools. The row now says outright that the criterion is still open, and its License cell gives no license, so acceptance criterion 2 also fails for this row.",
      "evidence": "Line 84: \"| Docs tooling (not shipped) | MADR 4.0; markdownlint-cli2; lychee; Mermaid syntax check ... | Not yet in research; CI-only, never distributed (OQ-tech-stack-and-libraries-5) | Acceptance criterion 1 open until the research addendum or an amendment lands | ... | [Style guide](../_meta/style-guide.md); no research URL yet (OQ-tech-stack-and-libraries-5) |\". Line 365: \"| OQ-tech-stack-and-libraries-5 | Docs tooling lacks research URLs and licenses | ... | Yes, for acceptance criterion 1 |\".",
      "fix": "Add a 'Documentation tooling' section to docs/_meta/research/go-libraries-runtime.md, or register a new research file in manifest research_files and add it to this entry's research[]. Give each tool its primary repository or release URL and its license: adr/madr (MADR 4.0), DavidAnson/markdownlint-cli2, lycheeverse/lychee, and the mermaid npm package used by scripts/verify-docs.mjs. Cite those URLs in the row's Source cell, put the licenses in the License cell, delete the 'Acceptance criterion 1 open' risk text, and close OQ-tech-stack-and-libraries-5. If the research phase is frozen, record a foundation amendment exempting CI-only, never-distributed tooling from the research-URL rule, and amend acceptance criterion 1 in the manifest to match. Until one of these happens the criterion cannot pass."
    },
    {
      "id": "L2-2",
      "section": "Dependency hygiene",
      "issue": "Carried over from the previous L2 verdict (then L2-4), now narrower. The wording problem is fixed: 'the one exception' is gone, and raft-boltdb/v2 and go-msgpack/v2 are provisional MPL-2.0 exceptions with a merge block. But acceptance criterion 3 requires every chosen library license to be known and either permissive or called out as weak copyleft. The Control Store row still links three chosen modules whose licenses the document says are unknown. The research is unchanged: go-libraries-runtime.md section 6 gives the license of hashicorp/raft only. Acceptance criteria 2 and 3 therefore cannot pass for the Control Store row.",
      "evidence": "Line 67 License cell: \"hashicorp/raft MPL-2.0 (named exception); others unconfirmed, provisional (OQ-tech-stack-and-libraries-10)\". Line 303: \"| `github.com/hashicorp/raft-boltdb/v2` | v2.4.2 | Unconfirmed; provisional (OQ-tech-stack-and-libraries-10) |\". Line 306: \"`go.etcd.io/bbolt` and raft's other transitive modules are unconfirmed too\".",
      "fix": "Add the LICENSE URLs for hashicorp/raft-boltdb (the v2 module), hashicorp/go-msgpack (v2) and etcd-io/bbolt, with their SPDX identifiers, to docs/_meta/research/go-libraries-runtime.md section 6. Then (a) put the confirmed licenses in the Control Store row's License cell, (b) mark each MPL-2.0 row 'Confirmed' in the MPL-2.0 exception table, and drop any row that turns out to be permissive, (c) name bbolt's license in the sentence at line 306, and (d) close OQ-tech-stack-and-libraries-10, or narrow it to raft's remaining transitive modules with a license-gate run as the confirmation. Do not assert any license that is not in research."
    },
    {
      "id": "L2-3",
      "section": "Library catalog",
      "issue": "New in this revision. The 'Concerns not yet selected' table puts `/metrics` exposition on port 9901 for both binaries. In the foundation pack, 9901 is the admin port of `ruralzd` only. `ruralz-control` has 8090 (REST API and Ruralz Console) and 8091 (Control Stream), and 01-system-overview.md gives it no admin port. As written, the table assigns a canonical `ruralzd` port to `ruralz-control`.",
      "evidence": "Line 98: \"| `/metrics` exposition on 9901 | `ruralzd`, `ruralz-control` | Planned (M1) | OQ-tech-stack-and-libraries-16 |\". Foundation pack section 2 default ports: \"`ruralzd`: 8080 HTTP, 8443 TLS (+ UDP 8443 for HTTP/3), 9901 admin (`/healthz`, `/readyz`, `/metrics`, ...). `ruralz-control`: 8090 REST API + Console, 8091 Control Stream (mTLS)\". 01-system-overview.md line 48: \"| Ruralz Control | ... | 8090 REST API and Ruralz Console, 8091 Control Stream (mTLS) |\".",
      "fix": "Change the row to: \"| Metrics exposition (`/metrics`) | `ruralzd` (admin port 9901); `ruralz-control` (port not assigned by the foundation pack) | Planned (M1) | OQ-tech-stack-and-libraries-16 |\". Widen OQ-tech-stack-and-libraries-16 to 'Which `/metrics` exporter, and where does `ruralz-control` expose it (8090 path or a new admin port)?', with Owner 'Observability owner', and propose the port as a foundation amendment if a new one is chosen. Note: the body is about 3,986 words against the 4,000-word maximum, so offset any added words."
    }
  ],
  "non_blocking": [
    {
      "section": "Library catalog",
      "suggestion": "Figure 1 labels a node \"net/http and x/net/http2\", but the HTTP/1.1 and HTTP/2 row says `x/net/http2` is limited to low-level APIs and 'none in use'. Relabel the node \"net/http\" (Figure 2 already uses that label), or add x/net/http2 only when OQ-tech-stack-and-libraries-2 keeps it."
    },
    {
      "section": "Library catalog",
      "suggestion": "gRPC proxying says each Route-derived procedure is \"`/pkg.Service/Method` or a service prefix\". The configuration model defines `match.grpc` as `{service, method}` with method optional, not as prefix matching (02-configuration-model.md Route example: \"grpc: {service: shop.cart.v1.CartService, method: GetCart}   # method optional\"). Write \"or a whole service when `match.grpc.method` is omitted\", so no new matching semantics are implied."
    },
    {
      "section": "Selection criteria",
      "suggestion": "Line 47 tracks both S6 exceptions under OQ-tech-stack-and-libraries-6, but OQ-6 covers only the two JOSE stacks. Either add the gorilla/websocket duplication to OQ-6 or cite the Version floors table (v1.5.3) as its tracking."
    },
    {
      "section": "Library catalog",
      "suggestion": "Foundation pack section 7 says the Semantic Cache runs \"over the same State Store connection\". The Semantic Cache row requires \"a separate rueidis client with dedicated connections\", and the Risk cell says vector commands \"MUST NOT share auto-pipelined connections\". OQ-tech-stack-and-libraries-7 discloses the difference, and it is technically sound, so this is recorded as a foundation amendment below rather than a blocking finding. Until the amendment merges, soften the Chosen cell to 'the Gateway's State Store connection settings, with a dedicated rueidis connection pool'."
    },
    {
      "section": "Summary",
      "suggestion": "The body is about 3,986 words by the manifest word_count_rule, against length_words max 4000. Fixes for L2-1 to L2-3 should replace text rather than add it. For example, move rationale detail out of the Semantic Cache and WASM runtime Risk cells."
    }
  ],
  "ac_results": [
    {
      "ac": "Library catalog has a row, with a source URL present in docs/_meta/research/, for every concern: WASM runtime (wazero), Plugin PDK conventions (Extism-style), HTTP/1.1 and HTTP/2 (net/http, x/net/http2), HTTP/3 (quic-go), gRPC ingress (connectrpc.com/connect) and upstream (grpc-go), telemetry (go.opentelemetry.io/otel with slog bridge), Control Stream (own gRPC with xDS-style ACK/NACK, not go-control-plane), Control Store (hashicorp/raft with BoltDB), State Store client (redis/rueidis), Semantic Cache (Redis 8 Vector Sets or valkey-search), GraphQL (wundergraph/graphql-go-tools v2), Kafka (twmb/franz-go), NATS (nats.go/jetstream), MQTT (eclipse-paho/paho.golang and mochi-mqtt), JOSE and OIDC (lestrrat-go/jwx/v4, coreos/go-oidc/v3), expressions (google/cel-go), authorization (OPA v1 Rego, cedar-go), tokenizer (tiktoken-go/tokenizer, estimates only), rate limiting (GCRA in Redis Lua plus local token bucket), and docs tooling (MADR 4.0, markdownlint-cli2, lychee, mermaid-lint).",
      "pass": false,
      "note": "Every concern has a row. A mechanical check found all 86 distinct URLs in the document inside go-libraries-runtime.md or go-libraries-protocols.md. The Docs tooling row (line 84) still has no research URL. See L2-1."
    },
    {
      "ac": "Each Library catalog row gives the chosen library, alternatives, rationale, license, risk and the ADR it links to where one exists.",
      "pass": false,
      "note": "All 28 rows fill every cell, and the ADR links match the manifest ADR paths. But two License cells give no license: the Docs tooling row says \"Not yet in research\", and the Control Store row says \"others unconfirmed\" for raft-boltdb/v2, go-msgpack/v2 and bbolt. See L2-1 and L2-2."
    },
    {
      "ac": "Every chosen library license is permissive and compatible with Apache-2.0 distribution; any weak-copyleft license (for example MPL-2.0) is called out with its risk and rationale.",
      "pass": false,
      "note": "hashicorp/raft MPL-2.0 is called out with its obligations, and paho.golang's EDL-1.0 election is recorded. The licenses of raft-boltdb/v2, go-msgpack/v2, bbolt and the docs tooling are still unknown. The document now says so honestly and blocks merge, but the criterion is not met. See L2-2."
    },
    {
      "ac": "The CGO_ENABLED=0 stance is consistent with wazero (pure Go, no CGO) and FIPS builds use GOFIPS140.",
      "pass": true,
      "note": "Static builds cites wazero's 'doesn't rely on CGO' and confirms pure Go only where research does; every other row stays 'Pending CI' until the G1 cross-build. The FIPS build uses GOFIPS140=v1.0.0 with GODEBUG=fips140=on, Planned (M5), and G3 exceptions are tabulated."
    },
    {
      "ac": "The minimum Go version is 1.26.",
      "pass": true,
      "note": "Version floor: \"The minimum Go version is 1.26\", with `go 1.26.0` in go.mod. The jwx v4 GOEXPERIMENT=jsonv2 interaction is handled by a floor CI job with GOTOOLCHAIN=local and a guard file, and OQ-4 is closed."
    },
    {
      "ac": "Machine checks: required_terms (26 literals), min_table_rows >= 18 in Library catalog, no_blank_cells in Library catalog, doc-level required_terms",
      "pass": true,
      "note": "`verify-docs.mjs --allow-missing` reports no errors for this file, only link warnings for ADRs and Wave 2/3 docs that do not exist yet. The catalog has 28 data rows. All required terms are present."
    },
    {
      "ac": "H2 outline matches manifest (Summary, Scope and non-goals, outline_h2 in order, Open questions last); required flowchart dependency-graph diagram present with caption; Summary at most 120 words; length 1500-4000 words",
      "pass": true,
      "note": "The H2s match exactly and in order. Figures 1 and 2 are flowcharts from cmd/ruralzd, cmd/ruralz-control and cmd/ruralz, each with 25 nodes or fewer and an italic caption above. The Summary is about 102 words. The body is about 3,986 words, inside the band but at its ceiling."
    },
    {
      "ac": "Foundation consistency (L2 lens): canonical names, forbidden aliases, ports, CLI commands, milestone tags, glossary terms, ADR references",
      "pass": false,
      "note": "The previous 'Tier' reuse and the invalid HTTP/3 milestone cell are fixed, and ADR-0003 is now cited. No forbidden aliases were found. The CLI commands (`ruralz bundle validate`, `ruralz plugin test`, `ruralz dev run`) are in the known list. The config-model anchor #validation-and-diff-semantics exists. OQ-system-overview-1 and OQ-system-overview-7 match 01-system-overview.md. One port conflict remains: 9901 is attributed to `ruralz-control` (L2-3)."
    }
  ],
  "foundation_amendments": [
    "Section 7 WASM runtime (ADR-0004): name the repository `github.com/wazero/wazero` and the Go module path `github.com/tetratelabs/wazero` (https://github.com/wazero/wazero/blob/main/go.mod). Resolves OQ-tech-stack-and-libraries-1.",
    "Section 7 HTTP stack (ADR-0009): '`net/http` (HTTP/1.1, HTTP/2, h2c via `Server.Protocols`) + `quic-go` for HTTP/3; `golang.org/x/net/http2` only for non-deprecated low-level APIs' (https://github.com/golang/go/issues/78064). Resolves OQ-tech-stack-and-libraries-2.",
    "Section 7 Expressions / authz (ADR-0011): record the cel-go import path as `cel.dev/cel-go` (https://github.com/cel-expr/cel-go/releases/tag/v0.32.0), and update the manifest acceptance criterion that names `google/cel-go`. Resolves OQ-tech-stack-and-libraries-3.",
    "Section 7 Control Store (ADR-0006): name the log store as `hashicorp/raft-boltdb/v2` on `go.etcd.io/bbolt`, not 'BoltDB' (https://github.com/hashicorp/raft-boltdb/blob/master/v2/go.mod), and list every admitted MPL-2.0 module as a named exception once research confirms its license.",
    "Section 7 Semantic cache: change 'over the same State Store connection' to 'on the same State Store deployment and connection settings, using dedicated connections separate from the auto-pipelined rate-limit and quota connections'. Also state that Semantic Cache is unavailable on State Store servers without Vector Sets or valkey-search (Dragonfly, Valkey below 9.0.1). Resolves OQ-tech-stack-and-libraries-7.",
    "Section 2 default ports: assign a Control Store Raft peer port on `ruralz-control` (the document proposes 8092; the alternative is ALPN multiplexing on 8091), decided with OQ-system-overview-7. Also state where `ruralz-control` exposes `/metrics`, since the foundation pack gives it no admin port."
  ]
}
