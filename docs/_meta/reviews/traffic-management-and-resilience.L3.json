{
  "lens": "L3 acceptance-audience",
  "verdict": "revise",
  "score": 8,
  "blocking": [
    {
      "id": "L3-1",
      "section": "Response caching (Figure 4)",
      "issue": "A performance figure in a diagram label has no (target) or (hypothesis) tag. Style guide section 4 requires a tag on the same line as every performance or scale figure, and this lens checks that rule. The prose version of the same lease deadline carries the tag (line 395, \"for the 5 s deadline (target)\"), but the Figure 4 line does not. `verify-docs` misses it because the numbers check skips fenced code. Every other figure in the body and diagrams is tagged, cites a measurement URL, or is a config example.",
      "evidence": "Line 421 (Figure 4, sequenceDiagram): \"N-)SS: take the partition lease, SET NX PX for the 5 s deadline\". Style guide section 4: \"Every performance or scale figure (latency, throughput, memory, connections, percentages) MUST carry `(target)` or `(hypothesis)` on the same line\". Line 395: \"A worker takes the partition's lease for the 5 s deadline (target)\".",
      "fix": "Line 421: change the label to \"N-)SS: take the partition lease, SET NX PX for the 5 s deadline (target)\". Mermaid is excluded from the word count, so the body stays at 6,494 of 6,500 words."
    }
  ],
  "non_blocking": [
    {
      "section": "Whole document (length)",
      "suggestion": "The verify-docs word-count rule puts the body at 6,494 words, against a maximum of 6,500. Any prose added in the next round must be offset by cuts. Candidate cuts: the percentage checks in Response caching rule 3, and the second example in Rate limiting / Per-Node ceiling (\"scaling 10 to 100 Nodes ... 34 minutes\"), which the Failure matrix scale-out row already states. Do not remove the notation paragraph, the breaker low-traffic sentence, the node_count_unknown and file-mode distinction, or the Ruralz Control rows. Each of those resolved a blocking finding in the earlier round."
    },
    {
      "section": "Failure matrix (\"New or restarted Node, established keys\" row); Rate limiting / Decision path",
      "suggestion": "Line 485 says a warming Node admits \"at the per-Node ceiling without GCRA\", and line 223 says \"meanwhile admitting at the per-Node ceiling\". Both describe the proposed OQ-traffic-management-and-resilience-20 option (a). Line 219 says option (c), wait for GCRA, stays the conforming behavior until that question closes. Add \"(proposed)\" to line 485, as line 484 already does, so operators do not size for local-only admission as if it were decided."
    },
    {
      "section": "Failure matrix (\"Ruralz Control unreachable\" and \"Key-rotation flood\" rows)",
      "suggestion": "A Control-mode Node restarted without a count takes the full limit as its ceiling (line 263), and local-only keys past the first-seen budget run at that ceiling without GCRA (line 214). If every Node restarts during a Ruralz Control outage, for example a rolling upgrade, a key can reach N x limit while the State Store is healthy, under the proposed OQ-20 path. Rows 484 and 496 each hold half of that bound. State it once in row 496, for example: \"local-only keys (proposed) up to N x limit each\"."
    },
    {
      "section": "Failure matrix (\"Hot URI, partition or entry\" row)",
      "suggestion": "Row 486 gives the hot-entry cost as \"1% of a shard at 1,000 Nodes\", which counts calls. Response caching rule 1 (line 387) shows that bandwidth is the tighter limit: about 131 MB/s, or 10% of a 10 Gb/s shard link, per hot 128 KiB entry. Quote the bandwidth figure in the row too, and say that it adds up per hot entry on the same shard, so operators size links correctly."
    },
    {
      "section": "Rate limiting / Per-Node ceiling; Failure matrix (\"Ruralz Control leader or Raft quorum lost\" row)",
      "suggestion": "\"while stale replicas still serve new Nodes\" (line 269) and \"while new Nodes still get Revisions\" (line 497) could be read as saying that newly created Nodes join during quorum loss. Pack 8.11 and Control plane and GitOps (\"Quorum loss stops writes, logins and Enrollment\") say only already-enrolled Nodes with no active digest get the last committed assignment. Write \"enrolled Nodes not yet counted\" so the scenario is exact."
    },
    {
      "section": "Rate limiting / Per-Node ceiling",
      "suggestion": "\"while GCRA fails each bucket is clamped to max(1, 2 x `requests` / N_published) tokens\" reads as if it also applies to declared ceilings, although the next sentence says declared ceilings admit N_serving x ceiling. Write \"each derived-ceiling bucket\"."
    },
    {
      "section": "Rate limiting / Decision path; Observability cross-reference",
      "suggestion": "Observability's `ruralz_ratelimit_decisions_total` results are allow, deny_local, deny_global and fail_open. None of them identifies a local-only admission or first-seen budget exhaustion, both proposed in OQ-traffic-management-and-resilience-20. Propose a result value, for example `local_only`, beside that open question so operators can see the path once it is adopted."
    },
    {
      "section": "Traffic shaping / KrakenD EE routing and traffic features; Circuit breakers",
      "suggestion": "https://www.krakend.io/features/ and https://www.krakend.io/docs/backends/circuit-breaker/ appear only in krakend-parity.md and krakend-config-model.md, not in this document's manifest research[] file (scalability-patterns.md). verify-docs accepts them because they are under docs/_meta/research/. For traceability, add krakend-parity.md to the manifest research[] entry, which acceptance criterion 2 needs in any case."
    },
    {
      "section": "Overload protection",
      "suggestion": "The section is still two sentences. When word budget allows, a compact list of the bounded resources and where each is defined would let operators act without reading several documents: Node in-flight units, buffer budget, per-Upstream semaphore and pending queue, retry budget, probe slots, key table, hot-entry layer, balancer budget and post-commit queue. Also list their Node memory figures (64 MiB key table, 64 MiB hot-entry layer, 256 MiB balancer budget, all target), which Data plane's worst-case sum does not yet include."
    }
  ],
  "ac_results": [
    {"ac": "Rate limiting contains an algorithm table comparing accuracy, cost, multi-node behavior and burst handling (ADR-0008).", "pass": true, "note": "Algorithm choice table (lines 202-208) has Accuracy, State Store cost, Multi-Node behavior and Burst handling columns and five rows, including the chosen hybrid. It cites ADR-0008 and uses URLs from scalability-patterns.md."},
    {"ac": "Every KrakenD EE routing and traffic feature (catch-all fallback, header and query dynamic routing, conditional routing, wildcard routes, URL rewrite, virtual hosts, client redirects, customizable HTTP circuit breaker, service rate limit, tiered rate limit, stateful rate limit, IP filtering, GeoIP) is mapped to a Ruralz mechanism with a milestone.", "pass": true, "note": "All 13 features appear in the KrakenD EE table (lines 456-468), each with a mechanism and a Planned (M1) or Planned (M2) tag. The circuit breaker row now says `max_errors` imports as `consecutiveFailures` with fidelity `approximate`, a level the Configuration model defines."},
    {"ac": "Failure matrix gives fail-open or fail-closed behavior for each dependency failure, including State Store outage.", "pass": true, "note": "Rows cover State Store slow, unreachable, shard, hot key, OOM, eviction, failover and Cell loss. They also cover Ruralz Control unreachable or no count, leader or quorum loss, file mode, discovery sources, Endpoints, zones, TLS, breaker and low-traffic Upstreams, and CEL errors. Every row has a fail-open or fail-closed cell, and the previous L3-2 gap is closed."},
    {"ac": "Rate limiting specifies the IETF RateLimit-Policy and RateLimit response header contract.", "pass": true, "note": "RateLimit response headers names draft-ietf-httpapi-ratelimit-headers-11 as RFC 9651 lists and gives the q, w, r and t semantics, per-code t values, Retry-After precedence, omitted pk and qu, and an example. Admitted-response headers are OQ-traffic-management-and-resilience-2, blocking for M1."},
    {"ac": "Quotas supports long windows and token-weighted units, including LLM tokens.", "pass": true, "note": "Windows of 24h and 720h are epoch-aligned. The `tokens` unit is covered through `ai.token-budget`, with the pack 8.9 reservation and settlement, including charging all of R when usage is missing."},
    {"ac": "Timeouts, deadlines, retries and hedging defines a retry budget.", "pass": true, "note": "Retry budget: max(3, 20% of in-flight originals) per Upstream per Node (target), with a counter and an amplification bound. Fields are OQ-5."},
    {"ac": "Response caching follows HTTP caching semantics including stale-while-revalidate.", "pass": true, "note": "Covers RFC 9111 shared-cache storability, freshness, stale limits, request directives, revalidation and invalidation. Also covers RFC 5861 stale-while-revalidate and stale-if-error, and RFC 9211 Cache-Status. Figure 4 shows the stale-while-revalidate flow."},
    {"ac": "Machine checks: required_terms (RateLimit-Policy, GCRA, token bucket, retry budget, stale-while-revalidate, (target)); has_table Rate limiting and Failure matrix; no_blank_cells Failure matrix; doc required_terms", "pass": true, "note": "verify-docs --allow-missing --skip markdownlint prints no FAIL line for this document. The only WARN lines are links to documents not yet written (ADR-0008, ADR-0011, ADR-0014, 11-, 12-)."},
    {"ac": "H2 outline equals Summary, Scope and non-goals + outline_h2 + Open questions, exactly and in order", "pass": true, "note": "Summary, Scope and non-goals, Load balancing, Service discovery, Health checking and outlier detection, Timeouts, deadlines, retries and hedging, Circuit breakers, Rate limiting, Quotas, Response caching, Traffic shaping, Overload protection, Failure matrix, Open questions."},
    {"ac": "Required Mermaid diagrams with captions: rate-limit decision flowchart, retry and breaker stateDiagram-v2, Endpoint selection flowchart", "pass": true, "note": "Figure 3 flowchart, Figure 2 stateDiagram-v2 and Figure 1 flowchart each have an italic caption directly above, plus an extra Figure 4 sequenceDiagram. Each diagram has at most 25 nodes."},
    {"ac": "Length band 2,500 to 6,500 words; Summary 120 words or fewer", "pass": true, "note": "The verify-docs wordCount rule gives 6,494 body words, 6 below the maximum. The Summary is about 77 words."},
    {"ac": "Every performance or scale number carries (target) or (hypothesis)", "pass": false, "note": "Figure 4 line 421, \"SET NX PX for the 5 s deadline\", is untagged (L3-1). All prose and table figures are tagged or cite a measurement URL."},
    {"ac": "Declared audience (architects, operators, contributors) can act without reading sources", "pass": true, "note": "The previous gaps are closed. The metric label is now `memory`, and the file-mode ceiling is marked not degraded. The Notation paragraph defines N_published, N_serving, N_frozen, TAT, tau and B before first use. Ruralz Control outage rows now exist, and the low-traffic breaker behavior is stated with an OQ-5 `minimumLegs` that blocks M1. Remaining clarity items are non-blocking."}
  ],
  "foundation_amendments": [
    "Pack 8.8 (proposed by this document, OQ-traffic-management-and-resilience-16): replace the derived ceiling limit / N with min(requests, max(10, ceil(2 x requests / N_published))) and a fail-open clamp of max(1, 2 x requests / N_published) per aligned window.",
    "Pack 8.8 (proposed by this document, OQ-traffic-management-and-resilience-20): allow first-seen keys past the per-Node budget to take local-only entries without GCRA while the State Store is healthy.",
    "Pack 8.11 (option (c) of OQ-traffic-management-and-resilience-19): allow a Node to persist the last published `clusterNodeCount` under ${RURALZ_DATA_DIR} so a Node restarted during a Ruralz Control outage keeps a derived ceiling."
  ]
}
